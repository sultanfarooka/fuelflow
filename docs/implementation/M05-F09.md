# M05-F09 — Account Head Management

> Implementation document. Created by `/feature-planning`. Tracks the phased
> plan and per-task status for this item. The matching `MODULES.md` status flip
> and this file ship in the **same PR** as the code (root `CLAUDE.md` Rule 1–2).

## Item

- **ID:** `M05-F09`
- **Legacy ID:** —
- **MODULES.md status:** `Planned` → `In Progress` (this PR). R01–R05 ship `Done`;
  R06 (entry-form picker filtering) defers to M05-F03/M05-F10, so the header rolls
  up to `In Progress` until those frontend features land.

## Dependencies

- `M12-F01` — `Done` ✓ (onboarding wizard; seeding hooks into Step 1 org-create and Step 2 fuel-type handlers)
- `M14-F01` — `Done` ✓ (AppDbContext / ControlPlaneDbContext split; `AccountHead` lives in `AppDbContext`)

No dependencies that are still `Planned`.

> **Note:** `M05-F11` (Financial Ledger) should be planned and shipped immediately after this PR. It creates the `FinancialEntries` table that all subsequent M05 and M15 features write to. `AccountHead.Id` becomes a FK in `FinancialEntries.AccountHeadId`.

## Summary

Provides a per-organization registry of income and expense account heads (Pakistani accounting term: *khatas*). Default expense heads are seeded when an organization is first created (onboarding Step 1); fuel-type income heads (`Fuel Sales PMG (Cash/Card)`, `Credit Sales PMG`, etc.) are seeded when fuel types are selected (onboarding Step 2). Owners and Managers can add custom heads (e.g., "Tyre Shop Rent") and rename or deactivate any non-system head. Account heads are consumed by **M05-F03** (daily expenses) and **M05-F10** (other income) as their categorization layer and by **M07-F03** (P&L report) for roll-up.

This PR covers **backend only**. Frontend account-head management UI ships in a follow-up PR alongside M05-F03 or M05-F10.

## Acceptance criteria (the test plan)

- **AC1** Given a new organization completes onboarding with PMG and HSD active, When seeding completes, Then four system income heads exist: `Fuel Sales PMG (Cash/Card)`, `Credit Sales PMG`, `Fuel Sales HSD (Cash/Card)`, `Credit Sales HSD` — and seven default expense heads are present.
- **AC2** Given an expense head with 0 existing transactions, When deactivation is requested, Then it succeeds and `IsActive` becomes `false`.
- **AC3** Given a system-managed head (`IsSystemManaged = true`), When deactivation is attempted, Then `400 Bad Request` is returned.
- **AC4** Given a custom INCOME head "Tyre Shop Rent" is created, When the list is queried with `?type=Income`, Then it appears in the result.
- **AC5** Given two custom heads with the same name in the same organization, When the second is created, Then `409 Conflict` is returned.

> Note: AC2's "0 transactions" check is always true in this PR because M05-F03 and M05-F10 are not yet built. The `IAccountHeadUsageChecker` service is implemented as a stub that returns `false`; it is extended when those features ship.

## Layers touched

- [x] Domain — `AccountHead` entity, `AccountHeadType` enum
- [x] Infrastructure — EF config + migration
- [x] Application — Commands, Queries, DTOs, Validators, `IAccountHeadSeeder` interface, `IAccountHeadUsageChecker` interface
- [x] Infrastructure — handlers + `AccountHeadSeeder` + `AccountHeadUsageCheckerStub`
- [x] Api — `AccountHeadsController` + seeding wired into existing onboarding handlers
- [ ] Frontend — not touched (backend-only PR; account-head UI deferred to M05-F03/F10)

## Out of scope

- Frontend account-head management UI (follow-up PR).
- Deactivation of system-managed heads — blocked by design, `400` returned.
- Hard-delete of any head — intentionally never supported; soft-delete only.
- Head re-ordering or grouping within a type.
- The actual consumption of `AccountHeadId` by M05-F03 or M05-F10 — those features will reference the heads created here.

---

## Phases

### Phase 1 — Domain

- [x] Add `AccountHeadType` enum to `FuelFlow.Domain/Enums/AccountHeadType.cs`
  - `Income = 1`, `Expense = 2`
  - *Acceptance:* file compiles, no warnings.

- [x] Add `AccountHead` entity to `FuelFlow.Domain/Entities/AccountHead.cs`
  - Properties: `Id (Guid)`, `Name (string, required)`, `Type (AccountHeadType)`, `Description (string?)`, `IsActive (bool, default true)`, `IsSystemManaged (bool, default false)`, `OrganizationId (Guid)`
  - Navigation: `Organization Organization`
  - Extends `BaseEntity` per existing domain conventions.
  - *Acceptance:* entity compiles; no EF attributes on the entity (config lives in the configuration class).

### Phase 2 — Infrastructure: EF Config + Migration

- [x] Add `AccountHeadConfiguration` to `FuelFlow.Infrastructure/Data/Configurations/PerTenant/AccountHeadConfiguration.cs`
  - Table name: `account_heads` (snake_case per codebase convention, not `AccountHeads`)
  - `Name`: `IsRequired`, `MaxLength(100)`
  - `Description`: `MaxLength(255)`
  - `Type`: stored as `int` via `HasConversion<int>()`; comment cites `AccountHeadType` enum values
  - Unique index: `(OrganizationId, Name)` — names unique per org regardless of type
  - **Deviation:** no global query filter. M14-F01 superseded EF global query filters with physical context separation — tenant isolation is enforced by handlers passing `OrganizationId` from `ICurrentUserService` into org-scoped repository queries (see `server/FuelFlow.Infrastructure/CLAUDE.md` "Control Plane vs PerTenant Context"). The impl-doc's original "global query filter" instruction was based on the older pattern.
  - Relationship: `HasOne(Organization).WithMany().HasForeignKey(OrganizationId).OnDelete(DeleteBehavior.Cascade)`
  - Lives in `Configurations/PerTenant/` namespace so `AppDbContext.ApplyConfigurationsFromAssembly` picks it up automatically (no manual `ApplyConfiguration` call needed).
  - *Acceptance:* `AppDbContext` compiles; `AccountHeads` appears in `DbContext.Model`.

- [x] Add `AccountHeads DbSet<AccountHead>` to `AppDbContext`

- [x] Add EF migration `AddAccountHeads` to `Migrations/Tenant/`
  - *Acceptance:* `Up()` creates `account_heads` table with all columns, unique index, and FK to `organizations`.

- [x] Apply migration to dev database
  - *Acceptance:* table present in DB (verified via `psql \d account_heads` — all columns, both indexes, cascade FK).

### Phase 3 — Application: Commands, Queries, DTOs, Validators, Interfaces

**DTOs** — `FuelFlow.Application/Features/Finance/AccountHeads/DTOs/`

- [x] `AccountHeadDto` — `Id`, `Name`, `Type (string: "Income"|"Expense")`, `Description?`, `IsActive`, `IsSystemManaged` *(placed under `DTOs/AccountHead/` + request DTOs, matching the codebase's `DTOs/{Feature}/` convention rather than `Features/.../DTOs/`)*

**Commands** — `FuelFlow.Application/Features/AccountHead/Commands/`

- [x] `CreateAccountHeadCommand(CreateAccountHeadRequest)` → `Result<AccountHeadDto>`
  - Validator on `CreateAccountHeadRequest`: Name required, ≤100 chars; Type must be 1 or 2.

- [x] `UpdateAccountHeadCommand(Guid, UpdateAccountHeadRequest)` → `Result<AccountHeadDto>`
  - Validator on `UpdateAccountHeadRequest`: Name required, ≤100 chars; description ≤255.

- [x] `DeactivateAccountHeadCommand(Guid)` → `Result<bool>` *(codebase uses `Result<T>`, not `Unit`)*

**Queries** — `FuelFlow.Application/Features/AccountHead/Queries/`

- [x] `GetAccountHeadsQuery(int? Type)` → `Result<List<AccountHeadDto>>`
- [x] `GetAccountHeadByIdQuery(Guid Id)` → `Result<AccountHeadDto>`

**Interfaces**

- [x] `IAccountHeadRepository` (`Interfaces/Repositories/`) — `GetAllAsync(orgId, type?, ct)`, `GetByIdAsync(id, orgId, ct)`, `ExistsByNameAsync(orgId, name, ct)`, `AddAsync(head, ct)`. *(Org-scoped methods replace the planned global-query-filter approach — see Phase 2 deviation. Update happens via EF change-tracking + `IUnitOfWork.SaveChangesAsync`, so no explicit `UpdateAsync`.)*

- [x] `IAccountHeadSeeder` (`Interfaces/Services/`) — `SeedDefaultExpenseHeadsAsync(orgId, ct)` and `SeedFuelTypeIncomeHeadsAsync(orgId, fuelTypeName, ct)`. *(Single fuel-type-name param, not a list — the onboarding hook fires per fuel type in `CreateFuelTypeCommandHandler`.)*

- [x] `IAccountHeadUsageChecker` (`Interfaces/Services/`) — `HasTransactionsAsync(accountHeadId, ct)`; stub returns `false` until M05-F11.

### Phase 4 — Infrastructure: Handlers + Seeder + Stub

**Handlers** — `FuelFlow.Infrastructure/Features/AccountHead/`

- [x] `CreateAccountHeadCommandHandler` — checks unique name via `ExistsByNameAsync`; returns failure "already exists" (→ 409 in controller) if duplicate; maps via a shared static `ToDto`. *(No Mapperly — the codebase hand-maps small DTOs; followed that pattern.)*

- [x] `UpdateAccountHeadCommandHandler` — loads head (org-scoped); re-checks name unique excluding self; updates `Name` + `Description`; saves via `IUnitOfWork`.

- [x] `DeactivateAccountHeadCommandHandler`
  - Returns failure (→ 400) if `IsSystemManaged = true`.
  - Calls `IAccountHeadUsageChecker.HasTransactionsAsync`; returns failure (→ 400) if true.
  - Sets `IsActive = false`; saves via `IUnitOfWork`.

- [x] `GetAccountHeadsQueryHandler` — org-scoped query with optional type filter; ordered `Name ASC`.

- [x] `GetAccountHeadByIdQueryHandler` — returns failure "not found" (→ 404) if not in current org (org-scoped repo query enforces tenant isolation).

**Seeder** — `FuelFlow.Infrastructure/Services/AccountHeadSeeder.cs`

- [x] Implement `IAccountHeadSeeder`:
  - `SeedDefaultExpenseHeadsAsync` — seven expense heads, `IsSystemManaged = false`.
  - `SeedFuelTypeIncomeHeadsAsync` — `Fuel Sales {name} (Cash/Card)` + `Credit Sales {name}`, `IsSystemManaged = true`. Includes a name-length guard (truncates very long custom fuel-type names to fit the 100-char limit — resolves Open Question 1).
  - Idempotent: filters out names already present for the org before inserting; saves its own changes (called post-commit by onboarding).

**Usage Checker Stub** — `FuelFlow.Infrastructure/Services/AccountHeadUsageCheckerStub.cs`

- [x] Implement `IAccountHeadUsageChecker` returning `false` always, with a `// TODO M05-F03, M05-F10, M05-F11` comment.

**DI Registration** — `FuelFlow.Infrastructure/DependencyInjection.cs`

- [x] Registered `IAccountHeadRepository`, `IAccountHeadSeeder`, `IAccountHeadUsageChecker` as scoped.

### Phase 5 — Api: Controller + Seeding Hooks

**Controller** — `FuelFlow.Api/Controllers/AccountHeadsController.cs`

- [x] `GET  /account-heads` — `[Authorize(Roles = "Owner,Manager")]`; optional `?type=Income|Expense`; `200` list.
- [x] `GET  /account-heads/{id}` — `200` or `404`.
- [x] `POST /account-heads` — `201 Created` (`CreatedAtAction` → `GetById`); duplicate name → `409`.
- [x] `PUT  /account-heads/{id}` — `200`; duplicate name → `409`; not found → `404`.
- [x] `DELETE /account-heads/{id}` — deactivates; returns `200 { success: true }`. *(Deviation: codebase DELETE convention returns `200 { success: true }` not `204` — matched `FuelTankController.Delete`. The MODULES.md AC only requires "succeeds", which 200 satisfies.)*
- [x] Status mapping: the `Result<T>` pattern carries only a string, so the controller's `MapFailure` classifies by message — "not found" → 404, "already exists" → 409, else 400. Honors AC3 (400) + AC5 (409).

**Seeding hooks in existing onboarding handlers**

- [x] `OnboardingCommandHandler` (Step 1 — org creation): after `CommitAsync`, calls `SeedDefaultExpenseHeadsAsync(org.Id, ct)`. Best-effort (try/catch + log) so a seeding failure never fails onboarding.

- [x] `CreateFuelTypeCommandHandler` (the handler behind `POST /fuel-types`): after each fuel type is saved, calls `SeedFuelTypeIncomeHeadsAsync(orgId, fuelType.Name, ct)`. Confirmed onboarding Step 2 (`StepFuelTypes.tsx`) routes every catalog or custom fuel type through `createFuelType` → this handler (resolves Open Question 2 for the onboarding path). Best-effort.

---

## MODULES.md edits required

- [x] Flip `M05-F09` to `In Progress` in the first commit. Rows R01–R05 → `Done` in this PR; **R06 stays `Planned`** (it requires the M05-F03/M05-F10 entry-form pickers, which are frontend and out of scope here — the API `?type=` filter that enables it is shipped). The feature header therefore rolls up to **In Progress**, not Done, until F03/F10 land.
- [x] Bump `Last Updated` date in the `MODULES.md` header.

## Open questions

- ~~**Fuel type name format in income head names**~~ — **Resolved.** The hook lives in `CreateFuelTypeCommandHandler` and uses `fuelType.Name` (the name the user entered, catalog or custom). A length guard in the seeder truncates names so `"Fuel Sales {name} (Cash/Card)"` stays within the 100-char `AccountHead.Name` limit.

- **Re-seeding on fuel type changes** — still open, deliberately out of scope. Because the income-head seeding rides in `CreateFuelTypeCommandHandler` (not just onboarding), an Owner who adds a fuel type *post*-onboarding via `POST /fuel-types` now also gets income heads automatically. The only uncovered path is editing/renaming an existing fuel type — no head rename follows. Flag as a follow-up when a fuel-type-edit flow is built.

## E2E verification (Playwright MCP)

Backend-only feature (no Frontend layer). The account-head endpoints have **no frontend consumer yet** — the UI is deferred to M05-F03/M05-F10 — so there is no browser journey to walk. Verification was done at the API + data layer against a throwaway Release instance of `FuelFlow.Api` (the user's dev servers were not running; per skill rules I did not start their `scripts/dev.ps1` servers — I ran a separate instance pointed at the same dev Postgres).

- [x] **Build** — full solution `dotnet build -c Release`: 0 errors.
- [x] **Format** — `dotnet format --verify-no-changes`: exit 0 (no diff).
- [x] **DB schema** — `psql \d account_heads`: all 9 columns present, unique index `(organization_id, name)`, secondary index on `organization_id`, cascade FK to `organizations`. Matches the migration.
- [x] **App boot / DI graph** — API booted through the composition root, built the full service provider (proving `IAccountHeadRepository`, `IAccountHeadSeeder`, `IAccountHeadUsageChecker`, and the modified `OnboardingCommandHandler` / `CreateFuelTypeCommandHandler` / `AccountHeadsController` all resolve), connected to Postgres, and ran the startup seeder. A missing/circular registration would throw during provider build — it did not.
- [x] **Swagger registration** — `GET /swagger/v1/swagger.json` lists `account-heads` and `account-heads/{id}`.
- [x] **Auth enforcement** — `GET /api/v1/account-heads` and `POST /api/v1/account-heads` both return **401** without an auth cookie (routing + `[Authorize(Roles="Owner,Manager")]` wired).
- **Deferred — authenticated business-logic walk (AC1–AC5):** the create→201 / duplicate→409 / type-filter / deactivate-system-managed→400 / seeding paths require an authenticated Owner with an onboarded org. Forging a byte-perfect ASP.NET JWT by hand risks a false 403, and no frontend surface exists to drive a real login. These ACs should be walked in the browser when M05-F03/M05-F10 wire the consuming UI, or covered by an integration test using `WebApplicationFactory`. Handler logic was code-reviewed against each AC.
- **Playwright spec:** N/A this PR — no browser-accessible surface. The `fuel-flow-web/e2e-tests/m05-f09.spec.ts` regression spec is deferred to the consuming frontend feature (M05-F03/M05-F10), which introduces the route a Playwright test can navigate to.

### Post-merge re-verification (M14-F02-F03 reconciliation)

After `main` (M14-F02-F03) was merged into this branch, the feature was re-verified against the new per-tenant-database architecture:

- [x] **Build** — `dotnet build` of the full API graph: **0 errors** (only pre-existing MimeKit/NU1902 + one unrelated nullable warning). Confirms the `newOrganization` compile error is gone and the accessor refactor compiles.
- [x] **Format** — `dotnet format --verify-no-changes` on `FuelFlow.Infrastructure`: exit 0 (no diff).
- [x] **Snapshot integrity** — `dotnet ef migrations has-pending-model-changes --context AppDbContext`: *"No changes have been made to the model since the last migration"* — the merged snapshot coherently reflects both `account_heads` and the M14 shim removals.
- [x] **Migration replay readiness** — Tenant migration order is `Initial → AddAccountHeads → DropCrossContextForeignKeys`, so `organizations` exists before the `account_heads` FK; `AddAccountHeads.Up()` is valid EF-executable PostgreSQL that now runs at tenant-provisioning time via `MigrateAsync`.
- [x] **Architecture conformance** — `AccountHeadRepository` and `AccountHeadSeeder` reviewed line-for-line against the canonical `BankAccountRepository` per-tenant pattern; both resolve the per-request tenant context via `TenantDbContextAccessor.GetContextAsync`, so their writes are flushed by the accessor-backed `UnitOfWork`.
### Live onboarding walk (Playwright MCP) — completed 2026-06-06

Walked end-to-end against the running dev stack (frontend :5173, backend :5035, Postgres container on host **5433**) through the real onboarding wizard, provisioning an actual per-tenant database (`tenant_<orgid>`). All five ACs pass:

- [x] **AC1** — Register → verify phone (OTP from dev console sender) → login → onboarding Step 1 (org "M05F09 Test Org", station, OMC=PSO) provisioned `tenant_11db3816…`; `psql` on that DB shows the **7 default expense heads** (`is_system_managed=false`). Onboarding Step 2 (selected **MS** + **HSD**) created the fuel types, and the tenant DB then shows the **4 income heads** — `Fuel Sales MS (Motor Spirit / Petrol) (Cash/Card)`, `Credit Sales MS (Motor Spirit / Petrol)`, `Fuel Sales HSD (High Speed Diesel) (Cash/Card)`, `Credit Sales HSD (High Speed Diesel)` (`is_system_managed=true`). Names reflect the actual fuel-type names (M05-F09-R02).
- [x] **AC2** — `DELETE /api/v1/account-heads/{id}` on a custom head ("Tyre Shop Rent") → **200**; subsequent `GET /{id}` shows `isActive=false`.
- [x] **AC3** — `DELETE` on a system-managed income head → **400** "System-managed account heads cannot be deactivated."
- [x] **AC4** — `GET /api/v1/account-heads?type=Income` → **200**; after `POST` of custom income head "Tyre Shop Rent" it appears in the filtered list.
- [x] **AC5** — second `POST` of "Tyre Shop Rent" → **409** "An account head named 'Tyre Shop Rent' already exists."

> **Prerequisite fixes uncovered by this walk (committed on this branch):**
> 1. Dev-stack tooling — `scripts/dev.ps1` (psql transaction split, ensure-DB-up, fail-loud), `migrate.ps1` two-context wrapper, and the dev DB host port moved to **5433** to coexist with a native PostgreSQL on 5432 (`fix(m14-f06)`).
> 2. **M14-F03 provisioning was broken** and blocked all onboarding — the Tenant `Initial` migration created FK constraints to the control-plane `omcs`/`fuel_types` tables, which don't exist in a physically-separate tenant DB, so `MigrateAsync` failed at the first migration (per-tenant onboarding had never actually run; M14's E2E was deferred). Fixed by removing those cross-context FKs from `Initial` (tenant DBs are self-contained; existence enforced at the app layer) and neutralising the corresponding drops in `DropCrossContextForeignKeys`; also fixed the compensation path's `DROP DATABASE` (was batched in a pipeline) (`fix(m14-f03)`).
- **Playwright spec:** still N/A for a navigable account-head UI (none exists yet — deferred to M05-F03/M05-F10). The walk above drove onboarding (browser) + the account-head API (authenticated `fetch`); a route-navigable `e2e-tests/m05-f09.spec.ts` lands with the consuming frontend feature.

## Implementation notes

- **M14-F02-F03 reconciliation (post-merge fix).** This branch was cut against the single-shared-database architecture, then `main` (carrying M14-F02-F03, the physical per-tenant-database pivot) was merged in. The text merge succeeded but left the feature semantically broken; it was reconciled to the new architecture as follows:
  - **`AccountHeadRepository`** now injects `TenantDbContextAccessor` (not `AppDbContext`) and resolves the per-request tenant context via `GetContextAsync` — mirroring the other 11 per-tenant repos. M14-F02 replaced the scoped `AppDbContext` registration with `AddDbContextFactory`, so a direct `AppDbContext` dependency no longer resolves and its writes would never be flushed by the accessor-backed `UnitOfWork`.
  - **`AccountHeadSeeder`** likewise injects `TenantDbContextAccessor`; its default-expense-head names and a `BuildExpenseHead` factory are now `public static` so onboarding can reuse the same source of truth.
  - **`OnboardingCommandHandler`** seeds the 7 default expense heads **inline into the freshly-provisioned tenant `AppDbContext`** (the direct `tenantCtx` it already opens for the first station), in a best-effort `try/catch`. This is the only place that can reach the new tenant DB before the `org_id` JWT is issued — `TenantDbContextAccessor` resolves the tenant connection from the JWT `org_id` claim, which doesn't exist yet during onboarding. The merge had spliced the old hook into the M14-F03-rewritten handler referencing an undefined `newOrganization` variable (a hard compile error); that splice was removed.
  - **`CreateFuelTypeCommandHandler`** is unchanged — it runs authenticated (org_id present), so `SeedFuelTypeIncomeHeadsAsync` works through the accessor.
  - **Migration replay.** M14-F03 provisions each tenant DB by running all `Migrations/Tenant/` migrations via `MigrateAsync`. Verified `dotnet ef migrations has-pending-model-changes` reports no drift (the merged snapshot reflects both `account_heads` and the M14 shim removals), migration order is `Initial → AddAccountHeads → DropCrossContextForeignKeys` (so `organizations` exists before the `account_heads` FK), and `AddAccountHeads.Up()` is valid EF-executable PostgreSQL that now actually runs at provisioning time.
- **No EF global query filter.** The original plan called for a global query filter on `OrganizationId`. M14-F01 removed global query filters in favour of physical context separation (per `server/FuelFlow.Infrastructure/CLAUDE.md`). Tenant isolation here is enforced by every repository method taking `organizationId` (sourced from `ICurrentUserService.OrganizationId` in the handlers) and filtering on it explicitly.
- **DELETE returns 200, not 204** — matched the existing `FuelTankController.Delete` envelope (`{ success: true }`). MODULES.md AC only requires "succeeds".
- **Status codes via message inspection.** `Result<T>` carries only a string; `AccountHeadsController.MapFailure` maps "not found"→404 and "already exists"→409. This is the minimal way to honour AC3/AC5 without expanding the shared `Result` type. If richer typed error codes are added project-wide later, this controller should adopt them.
- **Migration authored by hand.** The running dev server held the Debug build locks, so `dotnet ef migrations add` (which builds Debug) produced an empty migration. The migration + designer + snapshot entries for `account_heads` were written manually to match what EF would generate, and the table was created/verified directly in Postgres. The migration history row (`20260602010000_AddAccountHeads`) was inserted so `dotnet ef database update` is a no-op on this dev DB.
- **Seeding is best-effort.** Both onboarding hooks wrap the seeder in try/catch + structured log so a seeding failure never fails the primary operation (onboarding / fuel-type creation). Seeding runs after the primary transaction commits.
