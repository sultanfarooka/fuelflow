# M01-F05-R02 — Owner can create Manager users

> Implementation document. Created by `/feature-planning`. Tracks the phased
> plan and per-task status for this item. The matching `MODULES.md` status flip
> and this file ship in the **same PR** as the code (root `CLAUDE.md` Rule 1–2).

## Item

- **ID:** `M01-F05-R02` (primary) — also closes **`M01-F09-R07`** (per-user
  "require OTP before first login" toggle, which `MODULES.md` explicitly defers
  to "the M01-F05-R02 PR").
- **Legacy ID:** — (none)
- **MODULES.md status:** `Planned` → flip `M01-F05-R02` and `M01-F09-R07` to
  `In Progress` in the first work commit (done by `/feature-implementation`,
  **not** at planning time) → `Done` in the shipping PR.

## Summary

An **Owner** can create **Manager** users for their organization, assigning each
Manager one or more stations. A created Manager gets **full access scoped to their
assigned station(s)** — there is no per-module permission matrix in this slice.
Onboarding is **phone + OTP self-set password**: the Owner submits the Manager's
name, +92 phone (optional email) and station assignment(s); the Manager receives
an SMS invite code and self-activates by setting their own password, after which
their next login shows **only** the assigned station(s). The Owner can toggle OTP
off per user (default on) — when off, the account is created already phone-confirmed
and a one-time temporary password is returned for the Owner to relay.

## Dependencies

Other `MXX-FXX[-RXX]` items that must be `Done` first (all are):

- `M01-F09` — phone-first auth + OTP infra (`verify-phone`, `resend-otp`,
  `PhoneVerification`, `IOtpHasher`, `ISmsSender`, `OtpOptions`). **Reused**, not rebuilt.
- `M14-F01..F06` — per-tenant DB split (`ControlPlaneDbContext` / `AppDbContext`,
  `UnitOfWork` tenant→control-plane save order, `ITenantConnectionResolver`,
  `UserStation`). The cross-context write choreography depends on it.
- Stations exist (`M08-F01` / onboarding) — a Manager must be assigned ≥1 station.

## Acceptance criteria (the test plan)

These become `[Fact]` names like `M01_F05_R02_OwnerCreatesManager` /
`M01_F09_R07_OtpNotRequiredReturnsTempPassword`:

- **AC1 (R02 create)** — Owner POSTs a valid create-Manager → `AppUser` created in
  the control plane with `OrganizationId = Owner.org` + Manager role, `UserStation`
  rows in the **tenant** DB, invite OTP issued + SMS sent; success response.
- **AC2 (tenant guard)** — Owner passes a `stationId` not in their org → failure,
  **no writes** in either context.
- **AC3 (R02 scoping)** — After activation, Manager login returns **only** the
  assigned station(s); Owner still sees all stations.
- **AC4 (R02 authz)** — A Manager (or any non-Owner) calls `POST /users/managers`
  → `403 Forbidden`, no implementation detail leaked.
- **AC5 (uniqueness)** — Duplicate phone → failure; duplicate email (when provided)
  → failure.
- **AC6 (R07 default)** — `requireOtp` omitted ⇒ treated as `true`; Manager created
  pending, login blocked until activated.
- **AC7 (R07 off)** — `requireOtp=false` ⇒ Manager created
  `PhoneNumberConfirmed=true`, temp password returned once, immediate login works.
- **AC8 (activation)** — Manager submits phone+code+newPassword to `/auth/activate`
  → password set, phone confirmed, login succeeds.

## Layers touched

- [ ] ~~Domain (entities, enums)~~ — **none** (invite reuses `OtpPurpose.Signup`;
  pending state derived from existing Identity fields)
- [ ] Infrastructure — EF config + migration (**optional, tenant-only**: unique
  index on `UserStation(UserId, StationId)`)
- [x] Application (Command/Query, DTO, Validator, interface)
- [x] Infrastructure — handlers + repository write
- [x] Api (controllers)
- [x] Frontend (route, components, query hooks, Zod schema, i18n)

## Out of scope (thin-slice fences — do not build)

- **M01-F05-R03** Manager-creates-Custom-Users — fenced off by
  `[Authorize(Roles="Owner")]` on the new controller (Managers cannot reach it).
- **M01-F06** granular per-module permissions — no `Permission` entity / claim /
  policy, no `HasAccessToStation` service. Manager = full access scoped to assigned
  stations via the existing role attributes + the station list returned at login.
- Forced password-change-on-first-login enforcement (needs a new `AppUser` flag) —
  noted as a follow-up.
- Wiring the disabled onboarding `StepInviteManager.tsx` to this endpoint — a
  fast-follow once the endpoint exists.

## Resolved decisions (settled during planning)

1. **Invite OTP purpose:** reuse existing `OtpPurpose.Signup` → **no Domain change,
   no migration**; `verify-phone` / `resend-otp` keep working unchanged.
2. **Activation UX:** add **one** new anonymous endpoint `POST /api/v1/auth/activate`
   (phone + code + newPassword) that verifies the invite OTP, sets the password, and
   sets `PhoneNumberConfirmed=true` in **one step / one code**. This is the clean
   realization of the chosen "phone + OTP self-set password" model (avoids the
   awkward two-code verify-phone-then-reset-password-otp path). Resend uses the
   existing `/auth/resend-otp`.
3. **OTP-not-required path (toggle off):** create the Manager
   `PhoneNumberConfirmed=true` with an **auto-generated temporary password returned
   once** in the response (Owner relays it). No forced-change enforcement this slice.
4. **Plan `MaxUsers` limit:** **enforced** (mirror the station-limit pattern); counts
   all active org users toward the cap (`-1` = unlimited).
5. **Manager list endpoint:** returns **Managers only** (Custom users are R03).

## Phases

> Each phase is a vertical slice ordered so each builds on the last. Check tasks off
> as they land and verify. One PR per feature against `main`. Convention detail (EF
> config section order, controller pattern, CQRS naming, component patterns) lives in
> the scoped `CLAUDE.md` files — do not restate it here; the implementer loads it
> automatically.

### Phase 1 — Application: command/query, DTOs, validator, repo method

- [ ] `Application/DTOs/Users/CreateManagerRequest.cs` — `FullName`, `Phone` (+92),
  `Email?`, `List<Guid> StationIds` (≥1), `bool RequireOtp = true` (default true so
  an omitted field means required). — *Acceptance:* compiles; default verified by a
  validator/DTO test.
- [ ] `Application/DTOs/Users/CreateManagerResponse.cs` — `UserId`, `FullName`,
  `Phone`, `OtpRequired`, `Message`, `TemporaryPassword?` (populated only on the
  toggle-off path).
- [ ] `Application/DTOs/Users/ManagerListItemDto.cs` — `Id`, `FullName`, `Phone`,
  `Email?`, `IsActive`, `PhoneConfirmed` (= activated), `List<StationRefDto> Stations`
  (id + name). Add a small `StationRefDto` if none exists.
- [ ] `Application/DTOs/Auth/ActivateAccountRequest.cs` — `Phone`, `Code`, `NewPassword`.
- [ ] `Application/Features/Users/Commands/CreateManagerCommand.cs` —
  `record CreateManagerCommand(CreateManagerRequest Request) : IRequest<Result<CreateManagerResponse>>`.
- [ ] `Application/Features/Users/Queries/GetManagersQuery.cs` — `record GetManagersQuery() : IRequest<Result<List<ManagerListItemDto>>>` (org derived from `ICurrentUserService`, never a client param).
- [ ] `Application/Features/Auth/Commands/ActivateAccountCommand.cs` —
  `record ActivateAccountCommand(ActivateAccountRequest Request) : IRequest<Result<...>>`.
- [ ] `Application/Validators/CreateManagerRequestValidator.cs` and
  `ActivateAccountRequestValidator.cs` — structure/format only (see Validation).
- [ ] `Application/Interfaces/Repositories/IUserStationRepository.cs` — add
  `Task AddRangeAsync(IEnumerable<(Guid userId, Guid stationId)> rows)` (tuple sig
  keeps Application from referencing the Infrastructure `UserStation` type; staging
  only — commit via `IUnitOfWork`). — *Acceptance:* `dotnet build` of Application clean.

### Phase 2 — Infrastructure: handlers + repo write

- [ ] `Infrastructure/Repositories/UserStationRepository.cs` — implement
  `AddRangeAsync` (call `GetContextAsync()` to pin the tenant accessor so
  `UnitOfWork` flushes it; stage only, no `SaveChanges`). Add `RemoveByUserAsync` for
  compensation.
- [ ] `Infrastructure/Features/Users/Commands/CreateManagerCommandHandler.cs` — the
  orchestrator (choreography below). Inject `UserManager<AppUser>`,
  `ICurrentUserService`, `IStationRepository`, `IUserStationRepository`,
  `IPhoneVerificationRepository`, `IOtpHasher`, `ISmsSender`, `IUnitOfWork`,
  `IOptions<OtpOptions>`, `ISubscriptionRepository`, `ILogger`. **Mirror
  `RegisterCommandHandler`** for the AppUser-create + role + OTP-issue + SMS sequence.
- [ ] `Infrastructure/Features/Users/Queries/GetManagersQueryHandler.cs` — org users
  in the Manager role + their `UserStation` stations → `ManagerListItemDto`.
  Hand-build the DTO (Mapperly can't map the Infrastructure `AppUser` from Application).
- [ ] `Infrastructure/Features/Auth/Commands/ActivateAccountCommandHandler.cs` —
  verify the `Signup`-purpose OTP (reuse `IOtpHasher` + `IPhoneVerificationRepository`),
  set the password via `UserManager.AddPasswordAsync` (or reset-token path if a
  password already exists), set `PhoneNumberConfirmed=true`, consume the OTP.
- *Acceptance:* handler unit tests below pass; `dotnet build` clean.

**Cross-context create-Manager choreography** (no distributed transaction;
`UnitOfWork` saves **tenant → control-plane**):

1. Authorize + resolve `orgId`/`role` from `ICurrentUserService`; recheck
   `role == owner` (mirror `CreateStationCommandHandler`). Fail closed otherwise.
2. **Validate stations FIRST** (before any write): load by ids via
   `IStationRepository`; assert all exist, `OrganizationId == orgId`, active. This
   read also pins the tenant context. Foreign/missing/inactive → fail, **nothing
   written** *(AC2 guard)*.
3. Plan `MaxUsers` check (count active org users; `-1` = unlimited).
4. Phone-uniqueness (query `UserManager.Users`); email-uniqueness when provided.
5. Create `AppUser` (`UserName = Phone`, `PhoneNumber = Phone`, `Email`, `FullName`,
   **`OrganizationId = orgId`** — critical for tenant routing on the Manager's later
   login). OTP-required ⇒ `CreateAsync(user)` with **no password** +
   `PhoneNumberConfirmed=false`. Toggle-off ⇒ generate temp password,
   `CreateAsync(user, tempPassword)`, `PhoneNumberConfirmed=true`. `CreateAsync` fail
   → return (nothing else written).
6. `AddToRoleAsync(Manager)`. Fail → compensate `DeleteAsync(user)`, return failure.
7. Stage `UserStation` rows (tenant) via `AddRangeAsync`.
8. OTP-required only: stage `PhoneVerification` (`Purpose = Signup`) exactly like
   `RegisterCommandHandler`.
9. `await _unitOfWork.SaveChangesAsync()` (tenant → control-plane). On throw →
   compensate: `DeleteAsync(user)` + best-effort `RemoveByUserAsync` for the tenant
   rows; log; return failure.
10. OTP-required: send invite SMS (fire-and-forget, try/catch — do **not** fail the
    command on SMS error; Owner can trigger resend).
11. Return `CreateManagerResponse` (`TemporaryPassword` only on the toggle-off path).

> *Why safe:* all validation (1–4) precedes all writes (5–9); the only multi-write
> failure window (9) has a single deterministic compensation (delete the just-created
> `AppUser`), and the `AppUser` is useless to login until OTP + stations exist.

### Phase 3 — Api

- [ ] `Api/Controllers/UsersController.cs` — **new**. `[ApiController]`,
  `[Route("api/v1/users")]`, `[Authorize(Roles = "Owner")]` (class-level Owner-only).
  `POST /users/managers` → `CreateManagerCommand`; `GET /users/managers` →
  `GetManagersQuery`. Thin dispatch only; `{ success, data }` / problem-details on error.
- [ ] `Api/Controllers/AuthController.cs` — add `[AllowAnonymous] POST /auth/activate`
  → `ActivateAccountCommand`, inside the existing `auth-ip` rate-limit group.
- *Acceptance:* endpoints appear in Swagger; AC4 (non-Owner → 403) holds via the
  attribute + handler recheck.

### Phase 4 — Frontend (full stack)

- [ ] Route `fuel-flow-web/src/routes/settings/users.tsx` → `/settings/users`,
  Owner-only (`/settings` is already Owner-guarded; keep belt-and-suspenders
  `requireRoles([Owner])`).
- [ ] Nav entry in `components/layout/nav-config.ts` (`labelKey: nav.users`,
  `to: "/settings/users"`, `roles: [Owner]`) — never hard-code the sidebar.
- [ ] `lib/api/users.ts` — `createManager`, `getManagers` (+ barrel export from
  `lib/api/index.ts`); `lib/api/auth.ts` — add `activateAccount`.
- [ ] `lib/validators/users.ts` — `createManagerSchema` mirroring the
  FluentValidation rules (reuse the +92 phone regex from `validators/auth.ts`);
  `requireOtp` default true; `stationIds.min(1)`.
- [ ] `components/users/manager-list.tsx` — TanStack Query `['managers']`, shadcn
  `Table`, status badge Active/"Pending OTP" from `phoneConfirmed`, loading skeleton +
  empty state.
- [ ] `components/users/manager-create-form.tsx` — TanStack Form + Zod +
  `useMutation(createManager)`; station multiselect built from shadcn `Checkbox` rows
  over the auth store `stations` (no new dependency); OTP-required `Checkbox`
  default-checked with a toggle-off `Alert`; on toggle-off success show the returned
  temp password **once** (with copy button); `invalidateQueries(['managers'])`.
- [ ] Activation screen (public): reuse the existing verify-phone screen pattern —
  phone + code + new password → `activateAccount`.
- [ ] i18n: `users.*` + `nav.users` keys in `locales/en.json` and `locales/ur.json`.
- *Acceptance:* E2E journey below is green.

### Validation (FluentValidation ↔ Zod, same shape)

- `FullName` `NotEmpty`/≤200; `Phone` `NotEmpty` + `^\+92\d{10}$`; `Email` optional,
  valid when present; `StationIds` ≥1, distinct, no `Guid.Empty`; `RequireOtp` bool.
- Handler-only business rules (not in the validator): phone/email uniqueness,
  station-belongs-to-org + active, plan `MaxUsers`.
- Activate: `Phone` +92, `Code` 6 digits, `NewPassword` min 6 + at least one digit
  (mirror the existing password rule).

## Critical files (reuse / mirror — don't re-derive)

- `server/FuelFlow.Infrastructure/Features/Auth/Commands/RegisterCommandHandler.cs` —
  template for the AppUser-create + role + OTP-issue + SMS sequence to copy into
  `CreateManagerCommandHandler`.
- `server/FuelFlow.Infrastructure/Features/Auth/Commands/LoginCommandHandler.cs`
  (`GetStationsForUserAsync`) — already returns **only** the user's `UserStation`
  stations (Owner falls back to all). **Do not modify** — writing the rows is all
  that's needed for Manager scoping (AC3).
- `server/FuelFlow.Infrastructure/Repositories/UserStationRepository.cs` (add the
  write) and `Repositories/UnitOfWork.cs` (tenant→control-plane save order the
  choreography relies on).
- `server/FuelFlow.Infrastructure/Features/Station/Commands/CreateStationCommandHandler.cs`
  — Owner recheck + plan-limit pattern to mirror.
- `fuel-flow-web/src/lib/validators/auth.ts` (+92 regex / Zod conventions),
  `src/routes/settings/route.tsx` (Owner guard), `src/stores/auth-store.ts` (stations).

## Migration impact

- **Zero control-plane migrations** — pending/activation state is derived from
  existing Identity fields (`PhoneNumberConfirmed`, `PasswordHash`); no new `AppUser`
  column. Invite reuses `OtpPurpose.Signup` so `PhoneVerification` is unchanged.
- **At most one tenant migration** — optional unique index on
  `UserStation(UserId, StationId)` for re-invite idempotency:
  `server/db-migration-add.ps1 -Name AddUserStationUniqueIndex -Context Tenant`.

## Verification (end-to-end)

- **Unit (Application)** — `M01_F05_R02_CreateManagerRequestValidatorTests`: valid /
  missing name / bad phone / bad email / empty + duplicate `stationIds` /
  `requireOtp` default-true.
- **Handler (Infrastructure, mocked repos/UserManager)** —
  `M01_F05_R02_CreateManagerCommandHandlerTests`: non-Owner fail; no-org fail;
  foreign/missing/inactive station fail (**no AppUser created**); dup phone/email
  fail; happy OTP-required (no password, Manager role, N station rows, OTP issued +
  SMS, single `SaveChangesAsync`); happy toggle-off (`M01_F09_R07_*`: confirmed +
  temp password, no OTP); role-assign fail ⇒ user deleted; save throws ⇒ user
  deleted; `MaxUsers` cap fail.
- **Integration (real two-context test DB)** —
  `M01_F05_R02_CreateManager_Integration`: assert control-plane `AppUser` + role +
  OTP and **tenant** `UserStation` rows; cross-tenant station guard writes nothing;
  then real `LoginCommandHandler` — blocked pre-activation, and after `/auth/activate`
  login returns **only** the assigned stations.
- **E2E (Playwright MCP) — mandatory (Api + Frontend):** Owner → `/settings/users`
  → Add Manager (name/phone/station, OTP-required) → success + "Pending OTP" row;
  toggle-off variant shows the temp password once; no-station → inline validation
  error; if the harness allows a second actor, complete activation and confirm the
  Manager login lands on only the assigned station.
- **E2E spec:** `fuel-flow-web/e2e-tests/M01-F05-R02.spec.ts` —
  `npm run test:e2e -- M01-F05-R02` green.
- **Lint/build:** `dotnet format` produces no diff, backend build + tests green;
  ESLint + Prettier clean.

## MODULES.md edits required

- [ ] Flip `M01-F05-R02` (Owner can create Manager users) to `In Progress` (first
  work commit) → `Done` (shipping PR).
- [ ] Flip `M01-F09-R07` from `Planned · deferred to M01-F05-R02 PR` → `Done` (and
  update its AC6 note in `M01-F09`).
- [ ] If multi-station assignment is delivered here, note that it also satisfies
  `M01-F06-R03` (Manager assignable to multiple stations) — decide at PR time whether
  to flip `M01-F06-R03` to `Done` or leave it under the F06 follow-up.
- [ ] Update the `Last Updated` date in the `MODULES.md` header.

## Open questions

- None blocking. (The toggle-off temp-password handling and the optional
  `UserStation` unique index are decided above; revisit at implementation time only
  if the team disagrees.)

## Implementation notes

<Blank at planning time. The implementer appends decisions, deviations from the plan,
and anything a reviewer should know.>
