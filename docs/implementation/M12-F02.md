# M12-F02 — Onboarding Dev Bypass

> Implementation document. Created by `/feature-planning`. Tracks the phased
> plan and per-task status for this item. The matching `MODULES.md` status flip
> and this file ship in the **same PR** as the code (root `CLAUDE.md` Rule 1–2).

## Item

- **ID:** `M12-F02`
- **Legacy ID:** —
- **MODULES.md status:** `In Progress` (set at planning time) → `Done` when R01–R04 all ship in PR #13.

## Dependencies

Other `MXX-FXX[-RXX]` items that must be `Done` first:

- `M12-F01` — provides `Station.IsSetupComplete`, the dashboard route-guard scaffold, and the onboarding wizard chrome that this feature extends. **Bundled in the same PR (#13)** rather than waiting for merge, per user decision.

## Summary

A development-environment-only flag that relaxes the dashboard route guard from [M12-F01-R16](../MODULES.md#m12-f01--onboarding-wizard). Backend computes a `devBypassActive` boolean as `IHostEnvironment.IsDevelopment() && Features:OnboardingDevBypass == true`; the boolean rides on the existing `AuthResponse` (login, refresh, `GET /auth/me`). Frontend consumes it to (a) relax the `/dashboard` `beforeLoad` guard, (b) show a "Skip to Dashboard (dev only)" affordance in the wizard chrome, and (c) render a persistent dashboard banner when bypass is active and onboarding is incomplete. Wizard intra-step validation and backend endpoint preconditions are **not** relaxed — the bypass is purely an entry-gate concern.

## Acceptance criteria (the test plan)

From [`MODULES.md` M12-F02](../MODULES.md#m12-f02--onboarding-dev-bypass):

- **AC1** Given `ASPNETCORE_ENVIRONMENT=Production`, When `Features:OnboardingDevBypass=true` is set in env, Then `GET /auth/me` returns `devBypassActive: false`. _(Production-safety property — the C#-level `IsDevelopment()` check short-circuits even if config says otherwise.)_
- **AC2** Given `ASPNETCORE_ENVIRONMENT=Development` AND `Features:OnboardingDevBypass=true`, When an authenticated user calls `GET /auth/me`, Then the response includes `devBypassActive: true`.
- **AC3** Given `devBypassActive=true` AND `isSetupComplete=false`, When the user navigates to `/dashboard`, Then the dashboard renders without redirect.
- **AC4** Given `devBypassActive=true`, When the user is on any wizard step, Then a "Skip to Dashboard (dev only)" button is visible between the progress bar and step header; clicking it navigates to `/dashboard`.
- **AC5** Given `devBypassActive=true` AND `isSetupComplete=false`, When the dashboard renders, Then a banner "Dev bypass active — onboarding incomplete" is visible at the top.
- **AC6** Given `devBypassActive=true` AND `isSetupComplete=true`, When the dashboard renders, Then no bypass banner is shown.

## Layers touched

- [ ] Domain (entities, enums) — **not touched**
- [ ] Infrastructure — EF config + migration — **not touched**
- [x] Application (Command/Query, DTO, Validator, interface)
- [x] Infrastructure — handler
- [x] Api (controller) — `Options/FeaturesOptions.cs` + `appsettings*.json` only
- [x] Frontend (route guard, banner, skip affordance, i18n, state plumbing)
- [x] Docs (`MODULES.md` registration + this file)

## Out of scope

- Backend endpoint precondition relaxation — production-grade behaviour stays; dashboard handles empty/missing data gracefully.
- Wizard intra-step validation relaxation — user opted for route-guard-only.
- Per-user / per-organization bypass — env-var only.
- Audit logging of bypass use — could be a future R05 if needed.
- Translation of new strings into Urdu — empty stubs follow the existing [M08-F05-R05](../MODULES.md#m08-f05--system-preferences) deferred-translation convention.

## Phases

> Each phase is a vertical slice. Convention detail (Options pattern, DI, DTO,
> shadcn primitives, Zustand persist, TanStack Router `beforeLoad`) lives in the
> scoped `CLAUDE.md` files — not restated here.

### Phase 1 — Backend: FeaturesOptions + DI + appsettings

- [x] Create [`server/FuelFlow.Infrastructure/Services/Options/FeaturesOptions.cs`](../../server/FuelFlow.Infrastructure/Services/Options/FeaturesOptions.cs) (mirrors `OtpOptions.cs` shape with `OnboardingDevBypass: bool` + `SectionName = "Features"`) — *Acceptance met:* class compiles; section name matches `appsettings` key. **Deviation from plan:** placed under `Infrastructure/Services/Options/` (the canonical location alongside `OtpOptions` / `SmsGatewayOptions`), not `FuelFlow.Api/Options/` which is reserved for HTTP/cookie concerns.
- [x] Register in [`server/FuelFlow.Infrastructure/DependencyInjection.cs`](../../server/FuelFlow.Infrastructure/DependencyInjection.cs) next to `OtpOptions` registration — *Acceptance met:* `services.Configure<FeaturesOptions>(configuration.GetSection(FeaturesOptions.SectionName))` added in section 4c.
- [x] Add `"Features": { "OnboardingDevBypass": false }` to [`server/FuelFlow.Api/appsettings.json`](../../server/FuelFlow.Api/appsettings.json) — *Acceptance met:* default-off in production.
- [x] Add `"Features": { "OnboardingDevBypass": true }` to [`server/FuelFlow.Api/appsettings.Development.json`](../../server/FuelFlow.Api/appsettings.Development.json) — *Acceptance met:* local dev defaults bypass-on. **Note:** `appsettings.Development.json` is gitignored (line 22 of root `.gitignore`, same as `Jwt:Secret` and `Email:Password`). Teammates opt in on their own machine via `dotnet user-secrets set "Features:OnboardingDevBypass" "true"` from `server/FuelFlow.Api/` — added to [`docs/ENV-MAP.md`](../ENV-MAP.md) as a documented key.
- [x] `dotnet build server/FuelFlow.Api/FuelFlow.Api.csproj` clean — *Acceptance met:* 0 errors; 5 pre-existing warnings (MimeKit advisory, unrelated nullable in `OpenShiftCommandHandler.cs`).

### Phase 2 — Backend: IOnboardingBypassFlagProvider + auth response wiring

- [x] Create [`server/FuelFlow.Application/Interfaces/Services/IOnboardingBypassFlagProvider.cs`](../../server/FuelFlow.Application/Interfaces/Services/IOnboardingBypassFlagProvider.cs) with `bool IsActive { get; }` — *Acceptance met:* interface in Application layer, no Infrastructure deps. **Deviation from plan:** placed under `Interfaces/Services/` (canonical service-interface location) rather than the plan's `Services/` folder.
- [x] Implement [`server/FuelFlow.Infrastructure/Services/OnboardingBypassFlagProvider.cs`](../../server/FuelFlow.Infrastructure/Services/OnboardingBypassFlagProvider.cs) — `IsActive => _env.IsDevelopment() && _options.Value.OnboardingDevBypass`. *Acceptance met:* the `IsDevelopment() &&` short-circuit is in C# code at line 33.
- [x] Register `IOnboardingBypassFlagProvider` → `OnboardingBypassFlagProvider` as Scoped in `DependencyInjection.cs` — *Acceptance met:* line added in section 4c immediately after `Configure<FeaturesOptions>`.
- [x] Add `public bool DevBypassActive { get; set; }` to [`server/FuelFlow.Application/DTOs/Auth/AuthResponse.cs`](../../server/FuelFlow.Application/DTOs/Auth/AuthResponse.cs) — *Acceptance met:* field appears at the top level alongside `User`, `Organization`, `Stations`, `Subscription`.
- [x] Inject `IOnboardingBypassFlagProvider` into all three auth handlers — [`GetCurrentUserQueryHandler.cs`](../../server/FuelFlow.Infrastructure/Features/Auth/Queries/GetCurrentUserQueryHandler.cs), [`LoginCommandHandler.cs`](../../server/FuelFlow.Infrastructure/Features/Auth/Commands/LoginCommandHandler.cs), [`RefreshTokenCommandHandler.cs`](../../server/FuelFlow.Infrastructure/Features/Auth/Commands/RefreshTokenCommandHandler.cs) — and set `DevBypassActive = _bypassFlagProvider.IsActive` inside each handler's `BuildAuthResponse()` — *Acceptance met:* all three handlers updated; built clean.
- [x] **Additional fix discovered during implementation:** [`AuthController.cs`](../../server/FuelFlow.Api/Controllers/AuthController.cs) endpoints `Login` (line 79), `GetCurrentUser` (line 105), and `RefreshToken` (line 137) project the `AuthResponse` into anonymous objects with explicit field lists. Adding `DevBypassActive` to `AuthResponse` alone would have left the field stripped from the JSON. All three projections now include `data.DevBypassActive`.
- [ ] Smoke: hit `/api/v1/auth/me` in Development → response JSON contains `"devBypassActive": true`. Hit with `ASPNETCORE_ENVIRONMENT=Production` (and `Features__OnboardingDevBypass=true`) → response contains `"devBypassActive": false`. — *Acceptance:* both ACs visually confirmed during Phase 6 e2e walk.

### Phase 3 — Frontend: state plumbing

- [ ] Add `devBypassActive: boolean` to `LoginResponse` interface in [`fuel-flow-web/src/lib/api/auth/login.ts`](../../fuel-flow-web/src/lib/api/auth/login.ts) — *Acceptance:* TS types compile across consumers.
- [ ] Add `devBypassActive: boolean | null` to `AuthState` in [`fuel-flow-web/src/stores/auth-store.ts`](../../fuel-flow-web/src/stores/auth-store.ts); set in `setAuthState()`; clear in `logout()`; include in `partialize` so it persists across reloads — *Acceptance:* after login in dev, `localStorage.auth-store.state.devBypassActive === true`.
- [ ] `tsc -b` clean — *Acceptance:* no type errors.

### Phase 4 — Frontend: dashboard guard relaxation + banner + i18n

- [ ] Update guard condition in [`fuel-flow-web/src/routes/dashboard/route.tsx`](../../fuel-flow-web/src/routes/dashboard/route.tsx) line ~25: `(!devBypassActive && !stations?.[0]?.isSetupComplete)` — destructure `devBypassActive` from `useAuthStore.getState()` — *Acceptance:* AC3 — log in as a fresh Owner in dev → dashboard renders, no redirect.
- [ ] Add conditional banner between `</header>` (~line 124) and `<main>` (~line 126): visible when `devBypassActive && !stations?.[0]?.isSetupComplete`. Use shadcn `Alert` + `AlertDescription` + `AlertCircle` icon — *Acceptance:* AC5 — banner visible at top of dashboard in dev with incomplete onboarding; AC6 — banner hidden when `isSetupComplete=true`.
- [ ] Add `dashboard.devBypass.{bannerTitle, bannerDescription}` keys to [`fuel-flow-web/src/locales/en.json`](../../fuel-flow-web/src/locales/en.json) with English copy, and matching empty-string stubs to [`fuel-flow-web/src/locales/ur.json`](../../fuel-flow-web/src/locales/ur.json) per M08-F05-R05 convention — *Acceptance:* `useTranslation()` in the banner resolves correctly in English.

### Phase 5 — Frontend: wizard "Skip to Dashboard" affordance + i18n

- [ ] Insert conditional Alert with skip button between progress bar (~line 175) and step header (~line 178) in [`fuel-flow-web/src/routes/onboarding/index.tsx`](../../fuel-flow-web/src/routes/onboarding/index.tsx), visible only when `devBypassActive` — uses shadcn `Alert` with `<Link to="/dashboard">` + `Button` — *Acceptance:* AC4 — affordance visible on every wizard step in dev; click navigates to `/dashboard`.
- [ ] Add `onboarding.devBypass.{bannerTitle, skipButton}` keys to `en.json` + empty stubs in `ur.json` — *Acceptance:* button label and banner title resolve correctly.
- [ ] Onboarding-side guard at [`fuel-flow-web/src/routes/onboarding/route.tsx`](../../fuel-flow-web/src/routes/onboarding/route.tsx) lines 8–14 is **not touched** — already-complete users still get redirected to `/dashboard` — *Acceptance:* read-through confirms no change.

### Phase 6 — E2E verification via Playwright MCP

> Mandatory if PR #15 merges before this work lands. PR #15 adds Step 4.5 (e2e walk via Playwright MCP) to the feature-implementation skill. Best-effort if PR #15 hasn't merged.

- [ ] Run `scripts/dev.ps1` so both servers are up — *Acceptance:* `mcp__playwright__browser_navigate http://localhost:5173` returns a snapshot.
- [ ] Walk AC1–AC6 in a real browser via the Playwright MCP, recording findings in the `## E2E verification` section below — *Acceptance:* every AC ticks green or a fix-loop entry is logged.
- [ ] Codify the passing journey as `fuel-flow-web/e2e-tests/M12-F02.spec.ts` — one `test()` per AC, semantic selectors (`getByRole` / `getByLabel`) — *Acceptance:* `npm run test:e2e -- M12-F02` green against running dev servers.

### Phase 7 — Pre-PR checks + amend PR #13

- [ ] `dotnet format` clean — *Acceptance:* no diff produced.
- [ ] `cd fuel-flow-web && npx eslint .` + Prettier clean — *Acceptance:* no diagnostics.
- [ ] All Phase 1–6 boxes in this doc flipped per Step 4 of the feature-implementation skill — *Acceptance:* `grep -c '^- \[ \]' docs/implementation/M12-F02.md` returns only Out-of-Scope layer rows.
- [ ] Flip M12-F02 R01–R04 to `Done` in `docs/MODULES.md`; header `M12-F02` flips to `Done`; M12-F01 header stays `In Progress` because R18/R19 remain `Planned`; M12 module-index row stays `In Progress` accordingly — *Acceptance:* `grep "M12-F02" docs/MODULES.md` shows all `Done`.
- [ ] Update existing **PR #13** via `mcp__github__update_pull_request`: revise title to reflect bundled scope, extend Summary + MODULES.md Status Update + Test Plan with M12-F02 content — *Acceptance:* PR #13 description includes M12-F02 detail.
- [ ] Push final commits to `feat-m12-f01-onboarding-wizard` — *Acceptance:* PR #13 picks them up automatically.

## MODULES.md edits required

- [ ] Append `### M12-F02 — Onboarding Dev Bypass` section with R01–R04 and AC1–AC6 (set to `In Progress` at planning time)
- [ ] On PR finalize: flip R01–R04 to `Done`, header `M12-F02` to `Done`
- [ ] No change to M12-F01 header (stays `In Progress` due to R18/R19 `Planned`)
- [ ] `Last Updated:` already `2026-05-30` from the R18/R19 commit — confirm

## Open questions

None — all four decision points were locked via AskUserQuestion before plan approval.

## Implementation notes

_Blank at planning time. The implementer appends decisions, deviations from the plan, and anything a reviewer should know._

## E2E verification (Playwright MCP)

_Populated during Phase 6._
