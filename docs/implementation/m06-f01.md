# M06-F01 — Price Configuration

> Implementation document. Created by planning. Tracks the phased plan and per-task status for this item. The matching `MODULES.md` status flip and this file ship in the **same PR** as the code (root `CLAUDE.md` Rule 1–2).

## Item

- **ID:** `M06-F01`
- **Legacy ID:** `PR-001` (R01)
- **MODULES.md status:** `Planned` → flip to `In Progress` when work starts, `Done` in the shipping PR.
- **Backing surface:** the **Fuel Pricing** child route of the Station Configuration hub (`M08-F07-R06`, `/dashboard/station/$stationId/configuration/pricing`).

## Dependencies

- `M08-F07-R06` — Station Configuration list-menu hub + child routes (Done · merged in #35). The Fuel Pricing card currently lands on `<UnderDevelopment />` at `routes/dashboard/station.$stationId.configuration.pricing.tsx`; this feature replaces that placeholder.
- `M08-F08` — Fuel Type Management (Done). A station with no fuel types has nothing to price; the page must handle the empty state and link back to the Fuel Types panel.
- Shared `components/data-table/` (Done · merged in #35). M06-F01 is the **second adopter** — first reuse of the abstraction outside `FuelTypesPanel`. Following the convention is non-negotiable per `components/CLAUDE.md`.
- Backend scaffold **already exists** — see "Key model facts" below.

## Summary

Post-onboarding self-service management of a station's fuel prices for Owner + Manager, rendered as the second child of the Station Configuration hub. Replaces the `<UnderDevelopment />` placeholder. Users see a list of all the station's fuel types with their **current** price + when it became effective, can **set** a new price for any fuel type with an explicit effective date/time (closing the previous price atomically), and can drill into the **history** for any fuel type. Implements R01–R04. **Out of scope:** double-confirmation (M06-F02), notification fan-out (M06-F02), mid-shift handling (M06-F03), margins (M06-F04), customer special rates (M06-F05), promotions (M06-F06).

## Key model facts (from code review during planning)

- **`FuelPrices` is per-tenant** (`AppDbContext`, table `fuel_prices`). Columns: `Id`, `FuelTypeId` (Guid, cross-context plain ref to control-plane `FuelType`), `StationId`, `Price` (decimal), `EffectiveFrom` (DateTime UTC), `EffectiveTo` (DateTime? UTC, NULL = current/active).
- **R01 ("one active per fuel/station") is already enforced at the handler level.** `SetFuelPriceCommandHandler` calls `GetCurrentByStationAndFuelTypeAsync` and stamps the prior row's `EffectiveTo = newRow.EffectiveFrom` before inserting the new one. No DB constraint; matches the application-level pattern used by `StationShift` ([M04-F03-R01](../MODULES.md#m04-f03--open-shift)).
- Existing handlers / repo / controller / DTOs / Mediator wiring all exist:
  - Query: `GetFuelPricesByStationQueryHandler` — returns `List<FuelPricesDto>` for a station (all rows, current + historical), org-guarded.
  - Command: `SetFuelPriceCommandHandler` — org-guards, validates fuel-type exists, closes the active row, inserts the new one.
  - Repo: `IFuelPricesRepository` — `GetByStationIdAsync`, `GetCurrentByStationAndFuelTypeAsync`, `GetByIdAsync`, `AddAsync`, `Update`.
  - Controller: `[Authorize(Roles="Owner,Manager")] [Route("api/v1/stations/{stationId:guid}/fuel-prices")]` — `GET /` (list), `POST /` (set).
- Frontend lib already exists: `lib/api/stations/fuel-prices.ts` — `getFuelPricesByStation`, `setFuelPrice`, with matching DTO types.
- The setup-wizard pricing step (`station-setup/step2-prices.tsx`) is precedent for the per-fuel-type form pattern; reuse the visual semantics where it matches.

## Visual consistency with M08-F08 (NON-NEGOTIABLE)

The Fuel Pricing surface is the **second adopter** of the patterns we shipped on the Fuel Types panel. It must inherit them verbatim — not approximate them. Reviewing the two panels side-by-side at merge time should make it obvious they belong to the same product. Items below are the contract:

| What | Rule |
|---|---|
| **Page chrome** | Render `<ConfigBreadcrumb items=[Dashboard, Station Configuration, Fuel Pricing] />` inside `container mx-auto max-w-5xl px-4 py-6`. Same wrapper as `configuration.fuel-types.tsx`. |
| **Card shell** | `<Card className="overflow-hidden">` + `<CardHeader>` (`flex-row items-start justify-between gap-3` — title block on the start, primary action on the end) + `<CardContent className="p-0">`. Card title carries the **`IconTag`** Tabler glyph at `size-5 text-primary` — matches the icon the hub card uses for Pricing. |
| **Table shell** | DataTable lives flush inside `CardContent` (no nested border / shadow — the card is the container). First / last cells in every row get `ps-6` / `pe-6` so data lines up with the card title above. |
| **Header row** | Muted background, semibold foreground (`<TableHeader className="bg-muted/60 [&_th]:font-semibold [&_th]:text-foreground">`). The DataTable provides this — we supply the columns. |
| **Striped rows** | `[&_tr:nth-child(even)]:bg-muted/30` on `TableBody`. Provided by the DataTable. |
| **Loading state** | DataTable built-in skeleton (`isLoading` prop). Do not roll a "Loading…" text. |
| **Search input** | DataTable toolbar's built-in search box (`enableSearch` + `searchPlaceholder`). Same leading IconSearch + `ps-9` shape as M08-F08. |
| **Faceted filters** | `DataTableFilterDef[]`: **Source** (OMC / Custom) and **Has active price** (Yes / No). Same DropdownMenu + live counts. |
| **Source badge** | Reuse the same Custom (primary tint) / OMC (outline) chip — **extracted to `components/station-config/badges.tsx` in this PR** so both panels render identically. |
| **"Current price" cell** | When a row has an active price: `Intl.NumberFormat('en-PK', currency:PKR)` in `font-medium text-foreground`. When no active row exists: neutral muted badge `bg-muted text-muted-foreground` reading **"No active price"** — same visual weight + tone as M08-F08's "Not yet sellable" badge. |
| **"Effective since" cell** | Relative time (`"2 days ago"`) inside a `<Tooltip>` carrying the absolute UTC instant. Use `<TooltipTrigger asChild><span>…</span></TooltipTrigger>` per the wrapper-span rule from `FuelTypeToggle`. |
| **Row actions** | `<Button variant="outline" size="sm">` for **Set price** (`IconCurrencyDollar` or `IconCoin`) and **View history** (`IconHistory`). Same shape as `Rename` on Fuel Types — same outline-button weight, `me-1` icon spacing, start-aligned `Actions` header (not right-aligned). |
| **Mobile cards** | `renderMobileCard` mirrors `FuelTypesPanel` mobile shape: top row = fuel name + Source badge; middle = current price (large) + relative effective-since (small); footer = two action buttons full-width. **No kebab menu.** |
| **Set price dialog style** | `<DialogContent showCloseButton={false} className="gap-4 p-6 sm:max-w-md">`, `<DialogTitle className="text-base font-semibold">`, plain right-aligned footer (`<div className="mt-2 flex justify-end gap-2">…</div>` — **do not use `<DialogFooter />`**, it brings the muted bar + negative margins back), `variant="outline"` Cancel + filled primary Save. |
| **History dialog style** | Same minimal style. Inside: a compact `<Table>` (no DataTable — read-only single-fuel view, no sort/filter UI needed) with the same header / striped rules as the panel table. The currently-active row carries the success-tone `<Badge>` (the `bg-success/10 text-success` we just shipped in #35). |
| **Empty state** | `px-6 pb-6 text-sm text-muted-foreground` — identical typography to M08-F08's "No fuel types yet." Station with no fuel types → empty card body links to `…/configuration/fuel-types`. Station with no prices set → regular table with every row showing "No active price". |
| **Tokens only** | Same audit-grep rule as M08-F08 — no raw Tailwind palette colours. The `--success` token now exists (shipped in #35), so `bg-success/10 text-success` actually renders the intended green. |

If a pattern needs to deviate from M08-F08 for a justifiable reason (e.g. pricing rows can't be deactivated, so there's no `Switch`), say so explicitly in the panel file's header comment so the next reviewer can audit the divergence.

## Shared-component extractions (this PR ships them)

Now that two panels need the same chrome, lift the M08-F08 patterns out of `fuel-types-panel.tsx` so M06-F01 imports them, M08-F02 / M08-F03 inherit them later, and the visual contract is enforced by reuse — not by copy-paste discipline.

| Extract | From | To | Used by |
|---|---|---|---|
| `SourceBadge` (Custom-primary / OMC-outline chip) | `fuel-types-panel.tsx` | `components/station-config/badges.tsx` | Both panels (Source column) |
| `ConfigPanelCard` — `<Card overflow-hidden>` + `<CardHeader>` (icon + title + description + primary-action slot) + `<CardContent p-0>` | inline JSX in `fuel-types-panel.tsx` | `components/station-config/config-panel-card.tsx` | Both panels (page shell) |
| `formatPkr` / `formatRelative` | new | `lib/format/currency.ts` / `lib/format/relative-time.ts` | Both panels; M07 reports next |

These extractions are part of this PR's Phase 4 (frontend), not a separate follow-up. The diff in `fuel-types-panel.tsx` should be small (swap inline JSX for imports) and is covered by the existing Playwright `m08-f08.spec.ts`.

## Decisions (made during planning — confirm during implementation)

1. **"Set price" UX is one form, one effective-from field.** No "schedule" / "future-effective" pickers in v1 — just a `datetime-local` defaulted to *now*. The handler already accepts any `EffectiveFrom` (past, present, future) because it closes the prior row at that exact instant. We document the constraint in UI copy: "Prices effective from this moment forward, unless you change the date."
2. **Backdated prices are rejected.** New rule for this PR — validator returns 400 when `EffectiveFrom < UtcNow - 5 minutes` (the 5-minute window absorbs client clock skew). Reason: backdated prices would silently rewrite historical sales math when shifts close. The 5-minute slop comes from the `M04-F03` shift-open clock-skew handling.
3. **History view is a read-only Dialog**, not a separate route. Per-fuel-type, fetched on demand (the existing list endpoint already returns all rows, so no new endpoint — we filter client-side).
4. **Sellable badge is **not** duplicated here.** The "Sellable / Not yet sellable" state belongs to the Fuel Types panel (M08-F08). The Pricing page shows "No active price" inline for fuel types with no `EffectiveTo == null` row and a deep link to add one.
5. **DataTable is reused.** Columns: Fuel Type · Unit · Current Price · Effective Since · Last Set By · Actions. Faceted filters: Source (OMC / Custom — pulled from M08-F08), Has-active-price (yes / no). Mobile cards via `renderMobileCard`.
6. **Audit via Serilog now.** Add `_logger.LogInformation("AUDIT FuelPrice.Set …")` to `SetFuelPriceCommandHandler` mirroring the M08-F08 pattern. The persistent `AuditLog` table rides with `M01-F08` — R04 is satisfied, not deferred.
7. **Currency formatting.** `Intl.NumberFormat('en-PK', { style: 'currency', currency: 'PKR' })` from a tiny `lib/format/currency.ts` helper. Display as `Rs 285.50` (matching `step3-prices.tsx` precedent). Same helper will be reused by future modules (sales, reports, finance).
8. **"Effective Since" column shows relative time** ("2 days ago") with the absolute UTC instant in a tooltip. Format via a new `lib/format/relative-time.ts` helper (no new dep — uses `Intl.RelativeTimeFormat`).

## Acceptance criteria (the test plan)

| AC | Maps to | Description |
|---|---|---|
| AC1 | R01 + R03 | Owner on Fuel Pricing tab sees a row per fuel type for the station, each showing current price + effective-since, scoped to **this** station only (sibling stations of the same org show different prices). |
| AC2 | R02 | Owner clicks **Set price** on a fuel type → dialog opens with current price prefilled + `EffectiveFrom` defaulted to now. Owner enters a new price + clicks Save → API call succeeds, table refreshes, new row appears with the new price. |
| AC3 | R02 (negative) | Owner tries to set `EffectiveFrom` more than 5 minutes in the past → blocked by Zod inline + FluentValidation/handler 400 ("Effective date cannot be in the past."). |
| AC4 | R01 (negative) | The handler closes any prior active price for the same `(stationId, fuelTypeId)` atomically — verified by walking AC2 twice and checking the history dialog shows the prior row with `EffectiveTo` stamped at the new row's `EffectiveFrom`. |
| AC5 | R04 | Owner clicks **History** on a fuel type → modal opens listing every price ever set for that fuel type, newest first, with `EffectiveFrom` and `EffectiveTo` (or "Active" badge for the row with `EffectiveTo == null`). |
| AC6 | R02 + R03 (negative — auth) | Accountant role hits any of `GET /api/v1/stations/{id}/fuel-prices` or `POST /…/fuel-prices` → 403; frontend route guard redirects from `/configuration/pricing` to `/dashboard`. |
| AC7 | implicit | Every successful `POST /fuel-prices` writes a Serilog audit entry `AUDIT FuelPrice.Set …` with actor + station + fuel type + old price → new price. |

## Layers touched

- [ ] Domain — no changes (entity already complete)
- [ ] Infrastructure — no schema changes; add Serilog audit + clock-skew validation to handler
- [ ] Application — add `EffectiveFrom not-past` rule to `SetFuelPriceRequestValidator` (new file)
- [ ] Api — no changes (controller already complete)
- [ ] Frontend — replace `<UnderDevelopment />` with `<FuelPricingPanel />`, add the panel + Set / History dialogs, Zod schema, currency / relative-time formatters
- [ ] Docs — `MODULES.md` status flip, this file's check-offs

## Out of scope

- **M06-F02** (double-confirmation + role-restricted price changes + notification fan-out). M06-F02-R02 already overlaps with our `[Authorize(Roles="Owner,Manager")]` so that part is incidentally satisfied, but no double-confirm UI or notification queue ships here.
- **M06-F03** (mid-shift handling) — needs `StationShift` (M04) to exist first.
- **M06-F04** (margins) — needs OMC reference-price intake; separate planning item.
- **M06-F05 / M06-F06** (special rates, promotions) — out of scope.
- The persistent `AuditLog` table (M01-F08) — Serilog only for now.
- Per-shift / time-window pricing rules (M06-F03) — not in v1.

## Phases

> Each phase is a vertical slice ordered so each builds on the last. Convention detail (EF config order, controller pattern, CQRS naming, component patterns) lives in the scoped `CLAUDE.md` files — not restated here.

### Phase 1 — Application validator (Backend)

- [x] Add `SetFuelPriceRequestValidator : AbstractValidator<SetFuelPriceRequest>` in `FuelFlow.Application/Validators/` — rules: `FuelTypeId` not empty; `Price > 0`; `Price <= 10_000` (sane upper bound vs `decimal.MaxValue / 2` from plan — see implementation notes); `Price` ≤ 2 decimal places (Decision: open question 1, "yes"); `EffectiveFrom >= DateTime.UtcNow.AddMinutes(-5)`. — *Acceptance:* AC3 backend leg.

### Phase 2 — Backend audit + clock-skew tightening

- [x] Add a Serilog audit line at the end of `SetFuelPriceCommandHandler.Handle`: `"AUDIT FuelPrice.Set: user {UserId} set {FuelTypeName} at station {StationId} to {NewPrice} (was {OldPrice}, effective {EffectiveFrom})"`. — *Acceptance:* AC7.
- [x] Defensive: if `EffectiveFrom` is older than the active row's `EffectiveFrom`, return `Result.Failure("Effective date cannot precede the current price's effective date.")`. Edge belt-and-braces beyond the 5-minute window. — *Acceptance:* AC4 belt-and-braces.

### Phase 3 — Frontend formatters

- [x] `lib/format/currency.ts` — exports `formatPkr(amount: number): string` → `"Rs 285.50"`. Single `Intl.NumberFormat('en-PK', { style: 'currency', currency: 'PKR' })` instance. Also exports `formatPkrPlain` for the price `<Input>` default value.
- [x] `lib/format/relative-time.ts` — exports `formatRelative(date: Date | string): string` → `"2 days ago"`, plus `formatAbsoluteUtc(d)` for the tooltip. `Intl.RelativeTimeFormat` based, no external dep. Reads `document.documentElement.lang` lazily so it follows i18n.
- [x] `lib/validators/fuel-price.ts` — Zod schema mirroring backend rule (price > 0, ≤ 10,000, ≤ 2 decimals; effective-from not more than 5 min in the past). Exports `priceSchema`, `effectiveFromSchema`, and the composite `setFuelPriceSchema`.

### Phase 4 — Extract shared patterns + render the panel

- [x] Extract per "Shared-component extractions" table above:
  - [x] Lift `SourceBadge` + `StatusBadge` + `SellableBadge` out of `fuel-types-panel.tsx` → `components/station-config/badges.tsx`. Replace inline JSX in the Fuel Types panel with imports. Badge components now accept primitive props (`isCustom: boolean`, `isActive: boolean`, `isSellable: boolean`) instead of a `FuelTypeDto` so any panel can compose them without coupling to that DTO.
  - [x] Lift the `<Card overflow-hidden>` + `<CardHeader>` + `<CardContent p-0>` shell out of `fuel-types-panel.tsx` → `components/station-config/config-panel-card.tsx` exposing props `{ icon, title, description, action, children }`. Replace inline JSX in the Fuel Types panel with `<ConfigPanelCard>`.
  - [ ] Re-run the existing Playwright `m08-f08.spec.ts` to confirm the M08-F08 panel still passes — the refactor must be visually transparent. *(Deferred to /feature-e2e-testing at Step 4.5.)*
- [ ] Edit `routes/dashboard/station.$stationId.configuration.pricing.tsx` — replace `<UnderDevelopment />` with `<FuelPricingPanel stationId={stationId} />`. Keep the `<ConfigBreadcrumb />` header in place. — *Acceptance:* AC1 (page renders).
- [ ] Add `components/station-config/fuel-pricing-panel.tsx`. Composes `<ConfigPanelCard icon={IconTag} title="Fuel Prices" description=…>` wrapping the shared `<DataTable>` per `components/CLAUDE.md` "Shared DataTable" section — **do NOT** hand-roll a Table here. Columns + faceted filters per Decision 5 above. Mobile cards via `renderMobileCard`.
- [ ] Empty state — if the station has no fuel types at all, render a centered call-to-action: "No fuel types yet. **Set up fuel types →**" linking to `…/configuration/fuel-types`. Distinct from the "no active price" per-row state.

### Phase 5 — Set price dialog

- [ ] `<SetPriceDialog>` co-located in the panel file. Standard minimal modal style (matches M08-F08 dialogs): `showCloseButton={false}`, semibold title, plain right-aligned footer, outline Cancel + filled primary Save.
- [ ] Fields: read-only Fuel Type name + unit; new Price (number, suffix `Rs/{unit}`); Effective From (`datetime-local`, default = now, disabled past dates via Zod). Reuse `Field` / `Input` / `Label` per the form-field rules.
- [ ] Mutation via TanStack Query; on success: invalidate the prices query key + the fuel-types query key (so the Fuel Types panel's `HasActivePrice` flips). Toast success. — *Acceptance:* AC2.

### Phase 6 — Price history dialog

- [ ] `<PriceHistoryDialog>` co-located. Standard minimal modal style. Renders a compact `<Table>` (no DataTable abstraction here — read-only single-fuel filter, no sort/filter UI needed): columns Price · Effective From · Effective To · Set By (latter from `CreatedAt` until M01-F08 adds an actor column). Newest first. Active row badged "Active" (success-tone). Data filtered client-side from the same `getFuelPricesByStation` response — no new endpoint. — *Acceptance:* AC5.

### Phase 7 — Tests

- [ ] Backend: per repo convention, no xUnit project — handler invariants guarded at build time + Playwright E2E.
- [ ] Playwright E2E → `fuel-flow-web/e2e-tests/m06-f01.spec.ts`. Walks AC1–AC6 (AC7 is backend-only, verified by manual log scan during implementation).

## MODULES.md edits required

- [ ] Flip `M06-F01-R01` … `R04` to `In Progress` at work start, `Done` in the shipping PR.
- [ ] Update the `Last Updated` date in the `MODULES.md` header.
- [ ] No new requirements; no refinements / supersessions.

## Open questions

- **Currency precision:** prices are `decimal` in the DB, but Pakistani fuel prices are quoted to 2 decimals (e.g. `285.05`). Confirm: cap input at 2 decimal places (Zod `.multipleOf(0.01)`)? Proposed: yes.
- **Effective-from granularity:** `datetime-local` gives minute precision. Confirm: minutes is enough? Proposed: yes — fuel prices change at most once a day in practice.
- **Currency symbol in input:** put `Rs` as the leading addon, or in the label only? Proposed: leading addon on the input + label-only would feel redundant. Show as `[Rs] 285.50`.
- **Bulk-set:** for the onboarding-wizard flow, all fuels get an "opening price" at once. Confirm: this PR adds the **management** surface, *not* a bulk-set; users set one price at a time post-onboarding. Onboarding step 3 stays as-is. Proposed: yes.

## Implementation notes

- **Open-question defaults applied without prompt** (Auto Mode): currency at 2 decimal places (Q1 — yes), `datetime-local` minute granularity (Q2 — yes), leading `Rs` addon on the input (Q3 — yes), no bulk-set surface (Q4 — yes, onboarding step 3 stays the only multi-fuel form).
- **Phase 1 — `MaxPrice` capped at `10_000`** instead of `decimal.MaxValue / 2`. The doc's `decimal.MaxValue / 2` was a "any absurd ceiling will do" placeholder; ten thousand per litre is a much tighter human-readable sanity cap that still leaves four orders of magnitude of headroom over current Pakistani fuel prices (~Rs 280/L in 2026). If we ever need to lift it, the cap is a single constant in `SetFuelPriceRequestValidator`.
- **Phase 1 — 2-decimal check via `decimal.GetBits`.** `Math.Round(p, 2) == p` would also work but is fragile against `MidpointRounding` defaults. Reading the scale byte directly from `decimal.GetBits()[3] >> 16` is the explicit, well-defined way.
