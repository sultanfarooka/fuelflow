namespace FuelFlow.Domain.Enums;

/// <summary>
/// Optional label for a shift (e.g. Morning, Evening, Night). Plan shifts table does not require this; use for display.
/// </summary>
public enum ShiftNames
{
    Morning,
    Evening,
    Night
}