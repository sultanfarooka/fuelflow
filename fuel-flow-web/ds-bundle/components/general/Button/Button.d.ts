import * as React from 'react';

/**
 * Button — from fuel-flow-web@0.0.0.
 * @replaces button
 */
export interface ButtonProps {
  [key: string]: unknown;
}

export declare const Button: React.ComponentType<ButtonProps>;
